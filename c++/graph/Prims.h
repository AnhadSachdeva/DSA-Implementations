#ifndef PRIMS_H
#define PRIMS_H

#include <vector>

using namespace std;

class Prims{

    public:
    // Given an undirected, weighted, connected graph represented as an adjacency list, compute a Minimum Spanning Tree (MST) using Prim’s algorithm. 
    // The algorithm starts from an arbitrary node and greedily selects the minimum-weight edge that connects a visited node to an unvisited node, 
    // using a priority queue for efficiency.
    vector<pair<int, int>> prim(vector<vector<pair<int, int>>>& graph, int numNodes){

    }

};


#endif